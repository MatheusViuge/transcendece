# Use este arquivo apenas como referência para execução isolada do backend.
# O deployment completo utiliza o `.env.example` da raiz e Docker Compose.

DATABASE_URL=postgresql+psycopg2://edutech_user:change-me@localhost:5432/edutech_db
JWT_SECRET_KEY=change-me-with-a-long-random-secret
